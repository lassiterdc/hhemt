---
source_sidecar: sidecar_phase5_2026-04-29_2000.md
synced_at: '2026-05-01_140823'
prompt_doc_type: main_backup
---

### Phase Audit History


| Phase | Phase-Local Diff Files | Audit-on-Diff Violations | Resolution | Commit |
|-------|------------------------|--------------------------|------------|--------|
| 1 — report_config schema and analysis.run() hook | configs/reports/README.md, configs/reports/default_report_config.yaml, src/TRITON_SWMM_toolkit/analysis.py, src/TRITON_SWMM_toolkit/cli.py, src/TRITON_SWMM_toolkit/config/loaders.py, src/TRITON_SWMM_toolkit/config/report.py, src/TRITON_SWMM_toolkit/sensitivity_analysis.py, src/TRITON_SWMM_toolkit/toolkit.py, src/TRITON_SWMM_toolkit/workflow.py, tests/test_config_validation.py | 0 new ruff errors on touched code (1 new Optional[Path] flagged by UP045 fixed in same session; 7 pre-existing I001 in loaders.py left alone per scope rule). 14/14 tests pass in tests/test_config_validation.py including 8 new Phase-1 validators (F-I-6, F-I-7, Flag 7, Flag 14, Flag 17, cross-field CSV validation). | All DoD items satisfied: schema + default config + README authored; facade threading verified by grep across analysis.py / sensitivity_analysis.py / workflow.py (19 hit sites); Decision + Stipulation docs authored and committed on main (agentic-workspace b0c46c160). | 09399d7 |
| 3 — per-sim renderers stop gate | src/TRITON_SWMM_toolkit/report_renderers/{per_sim_conduit_flow.py, per_sim_peak_flood_depth.py}, src/TRITON_SWMM_toolkit/report_renderers/_figure_emission.py (`collect_per_sim_source_paths` helper for the Snakemake function-based `params:`), src/TRITON_SWMM_toolkit/workflow.py (`_build_plot_rule_block_per_sim` method + `rule all` extension with `expand("plots/per_sim/{event_id}/{conduit_flow,peak_flood_depth}.png", event_id=SIM_IDS)`), tests/fixtures/synthetic_model/cache.py (`rainfall_peak_mm_per_hr 100→500`), tests/fixtures/synthetic_model/swmm_template.py (`perc_imperv_map → 100/100/100`, `_CONDUIT_DIAMETER_M 0.2→0.1`) | Phase 3 doc instructed `proc.scen_paths.swmm_hydro_inp` for conduit geometry, but `hydro.inp` is the hydrology-only SWMM variant with no `[CONDUITS]` section — produced a blank figure (iter-1 user feedback). Fixed: read `swmm_hydraulics_inp` (with `swmm_full_inp` fallback). Iter-1 also surfaced 4 visual feedback items (no conduits displayed, missing black outline, perceptually-uniform colormap on both panels, conduits not all surcharging); iter-2 applied the renderer fixes (`Blues`/`Reds` non-overlapping cmaps, two line artists per conduit for black underline + colored fill, draw all conduits regardless of value); iter-3 applied scenario tuning to push every conduit ≥ max-over-full. TRITON summary zarr has no `rio.crs` on the synth fixture — patched the watershed-reprojection branch to skip when raster has no CRS. | All Phase 3 DoD items satisfied: `per_sim_conduit_flow.py` and `per_sim_peak_flood_depth.py` produced with uniform `render(analysis, report_cfg, output_path, *, event_iloc)` signature; both renderers dispatch on `_get_enabled_model_types()` with model-type-skip placeholders; Snakemake per-sim rules wired into `rule all` via `expand()`; pytest tests/test_synth_04_multisim_with_snakemake passes 6/1 (skipped scheduler-context test) in 68s; pytest tests/test_synth_02 + test_synth_05 pass 7/7 in 86s after fixture rebuild. Per-sim figures iter-1 STOP-gate (peak_flood_depth) + iter-3 STOP-gate (conduit_flow) approved by user (2026-04-27). Surcharge confirmed empirically: max_over_full_flow = [1.00, 1.00, 1.07, 1.03] for C1/C2/C3/C4. Pre-existing stipulation `report renderers accept uniform signature` covers the per_sim renderers without amendment. | toolkit `5fe677e` (committed + pushed to `worktree-toolkit_04-23_1737_visualization-and-reporting-multiphase-pwi` 2026-04-27 12:43 EDT, 6 files, 388 insertions, 4 deletions; QAQC: 0 new ruff errors on touched code after 2 NEW E501 in peak_flood_depth.py:112-113 fixed inline by extracting `da_masked.max().compute()` and `da_masked.notnull().sum().compute()` to locals; pytest synth_02 + synth_05 7/7 in 81.86s; audit-on-diff N/A for toolkit-only diff scope); agentic-workspace commit deferred — aw repo carries Phase 2 closeout work (still uncommitted from 2026-04-26) + Phase 3 closeout in flight + Phase 4 substrate just authored; will land as one combined aw commit after Phase 4 implementation session |
| 2 — system map renderer stop gate | src/TRITON_SWMM_toolkit/report_renderers/{__init__.py, _cli.py, _figure_emission.py, system_overview.py}, src/TRITON_SWMM_toolkit/workflow.py (`_build_plot_rule_block_system_overview` + `rule all` extension), src/TRITON_SWMM_toolkit/scenario_inputs.py (empty-list guard in `update_hydraulics_model_to_have_1_inflow_node_per_DEM_gridcell`), tests/fixtures/synthetic_model/{cache.py, geometry.py, swmm_template.py, vectors.py} (iter-2 topology + sea-wall row + identical 5×5 subcatchments + smaller pipes), agentic-workspace `scripts/png_to_vault_webp.py` (workspace tooling, not toolkit) | swmmio's case-duplicated `[POLYGONS]` section bug — fixed by removing the empty placeholder from STARTER_INP. GDAL `to_raster` crash via swmm.toolkit library-load-order — worked around by driving fixture rebuilds through pytest. Iter-2 Improvement-agent false-negative on the sea-wall objective — overruled after main-agent direct verification of the DEM raster (row 26 all 50m) and QC's independent visual confirmation that the grey stripe is visible. Long-running orphaned `python -c` background process from a prior tool call kept rebuilding the synth fixture cache mid-iteration — diagnosed and killed (PID 2343598). | All DoD items satisfied: `system_overview.py` renders 2-panel (SWMM elements + TRITON DEM) figure with full-res + preview + manifest emission; CLI dispatcher present and exercises uniform signature; Snakemake `plot_system_overview` rule wired into `rule all`; pytest tests/test_synth_02 + test_synth_05 pass 7/7 in 88s with iter-2 fixture topology (4 junctions J1-J4 + sewer_outflow + dummy_outfall, 4 conduits, 3 identical 5×5 subcatchments draining to J1/J2/J3, sea-wall row at DEM matrix_row 26); system_overview iter-1 + iter-2 STOP-gate approved by user (iter-2 closed 2026-04-26 10:15 EDT). Stipulation `report renderers accept uniform signature` authored at agentic-workspace `library/docs/stipulations/TRITON-SWMM_toolkit/`. v1.5 amendment block (11 lessons) added to the in-flight Data Viz Review Algorithm in the session scratch doc. | toolkit `dddc0e0` (committed 2026-04-26 11:30 EDT, 10 files, 1008 insertions, 91 deletions); agentic-workspace commit deferred per user (2026-04-26 11:35 EDT — "fine to skip AW commit right now") — 4 AW-side files (stipulation, master-plan File-by-File update, moved phase doc, `scripts/png_to_vault_webp.py`) remain in working tree pending resolution of pre-existing AW repo state (76 stale staged files + 6 unmerged files; details in scratch § "Commit results") |



# Follow-up Ideas


- Worktree-slug derivation ambiguity: for workspace names containing underscores (e.g., `TRITON-SWMM_toolkit`), `_derive_worktree_slug` in `scripts/generate/setup_pwi_worktree.py` strips at the first underscore (mechanical), while a semantic read of `worktree isolation.md` § Step 2 / Naming convention strips the full workspace name. The two produce different slugs. Options: (a) clarify the protocol wording to explicitly say "strip up to and including the first underscore," (b) add a shared helper that both the script and the `EnterWorktree` invocation docs in the protocol reference, (c) add a protocol-side worked example for workspaces with underscored names. Encountered during `/proceed-with-implementation` 2026-04-23 1737 visualization-and-reporting session — lost ~5 tool calls recovering.
- Baseline audit scalability for code-only plans: `pwi_audit_snapshot.py` runs `scripts.audit` over the full prompt-doc tree, which took >3 min without completing on this TRITON-SWMM_toolkit session. For plans with `modifies_prompts: false` or narrow prompt-doc surface (e.g., this plan modifies only 2 prompt docs in Phase 6), a targeted `--files` baseline would produce a meaningful delta in seconds. Consider adding `--files` or `--scope <workspace>` support to `pwi_audit_snapshot.py` so the baseline gate scales with surface size, not tree size.
- Dedicated data-viz subagent (wrapup `/plan-implementation` target): stand up a data-vis-review specialist agent with its own prompt-doc-backed rubric and its own scratch-doc composition, replacing the generic-subagent + verbatim-rubric pattern from the `# Data Viz Review Algorithm` section. Triggered at the end of this `/record-process` session once the algorithm, rubric, subagent-invocation template, round checklist, and iteration-record template have stabilized through real iteration rounds. The prompt doc + `/create-skill`-built skill produced during process-recording become the input substrate for the agent spec.

%%

"#" + "followup" tag: When the user tags an inline comment with `#` + `followup`, cut the tagged text from its original location, paste it here as a bullet, and add context sub-bullets if the original text does not stand alone without its surrounding context. The agent judges whether sub-bullets are needed — if the comment makes sense on its own, no sub-bullets are required. Change the tag in the original location to `#followup-moved`.

Specialist findings routed here by /invoke-specialist Step 6 appear under:

  ### Specialist-identified follow-up items
  #### {specialist-name}

These are appended via append_scratch_section.py and are automatically picked up by /review-followup-ideas.

Agents must never recommend action items for followup unless they are orthogonal to the objectives and target outcomes of a session.

%%

### Specialist-identified follow-up items

#### triton-swmm-toolkit-specialist

- **Planning-Instruction Improvement Proposal 1**: Add a "Substrate attribute/method verification" atomic to /plan-implementation that, for every phase doc referencing `scen_paths.<attr>`, `analysis.<method>`, or `sensitivity.<method>`, runs `grep -n "<attr>|<method>"` against the current source and fails the phase-doc writing step if the reference does not match. Would have caught Flags 1, 2, 3, 5 at plan-authorship time in this session.
- **Planning-Instruction Improvement Proposal 2**: Expand the master-plan "File-by-File Change Plan" atomic to require a "Facade threading audit" row whenever a new runtime parameter is added to `analysis.run()` — explicitly enumerating the 4 facade layers from architecture.md § submit_workflow() Facade Layers and asserting a change (or an explicit "no change needed because {reason}") at each layer. Would have caught Flag 4.
- **Planning-Instruction Improvement Proposal 3**: Add a "classmethod surface verification" atomic that, when `_cli.py` or any runner script references `ClassName.from_*()` classmethods, grep-validates those classmethods exist. Would have caught Flag 5.
- **Planning-Instruction Improvement Proposal 4**: Expand /plan-implementation's Validation Plan atomic to require one idempotence test for any new Python facade method advertised as idempotent. Would have caught Flag 13 / R11 case.


## Specialist-identified follow-up items

### triton-specialist

- **Extend `process_simulation.py::_export_TRITONSWMM_TRITON_outputs` to write a `dt_history` 1D zarr variable** parsed from `log_run_triton.log` / `log_run_tritonswmm.log` at processing time. The same pass should write `it_count_history`, `n_dt_floor_hits`, `min_dt_reached` as scalar/1D summary variables. Eliminates F-T1's runtime log-parsing dependency and converts dt-history items from "parse-on-demand" to "read-from-zarr". Belongs in toolkit specialist's plan landscape.
- **Add `max_cfl`, `max_froude`, `max_speed_m_s` as cell-wise summary zarr variables** computed during `process_simulation.py`'s reduction pass over raw `H,QX,QY` timeseries. Enables F-T3, F-T4 without requiring report renderers to hold raw timeseries in memory.
- **Per-rank performance disaggregation in the toolkit's performance zarr** — F-T2 needs per-rank named-timer values; if `performance.zarr` aggregates across ranks, F-T2 cannot render. Audit `process_simulation.py` to confirm per-rank timers are preserved or extend it.
- **Knowledge doc candidate (deferred — skill-gate constraint in subagent context)**: `library/knowledge/triton/triton solver health diagnostic rubric.md` — synthesizes the ★-tier smell-detection rubric (dt(t), per-rank wallclock, max-value outlier audit, ensemble health) with source-data → diagnostic → failure-mode mapping. Body fully drafted in triton-specialist scratch §"Specialist Response" + §"Info-Doc Pass Actions"; main agent should invoke `/write-knowledge` to land the file.
- **Knowledge doc candidate**: `library/knowledge/triton/raw vs summary output schema.md` — clarifies what's in `TRITONSWMM_TRITON_summary.zarr` vs `TRITON_only_timeseries.zarr` vs raw `H/QX/QY/MH` binaries vs `performance*.txt` vs `log_run_*.log`.
- **Substrate refresh recommendation**: the staleness check flagged TRITON architecture as 14 commits behind. Run `manage-substrate` against the upstream pin before the v2 plan commits to renderer count, OR accept the risk that one or two proposed renderers may be obsoleted by upstream additions.
- **Restructuring proposal for the v2 plan's applicability vocabulary**: add a `T+raw` applicability code (or a `requires_raw_timeseries: true` field) to the `report_config` Pydantic schema so renderers gracefully no-op when only summary outputs are available. Without this, F-T3, F-T4, F-T5 will fail at render time on summary-only configurations.
- **Process observation**: the seed catalog's `T-` prefix conflicts with the natural "TRITON-specific" prefix `T-T*`. The applicability codes (U/T/S/TS/CC) and the kind codes (T-/F-) sharing a letter is a real readability signal. Consider renaming kinds to `Tab-` and `Fig-` in v2.

### swmm-specialist

- **Extend `swmm_output_parser.py::parse_rpt_single_pass` to also extract WARNING/ERROR lines, the Time-Step Critical Elements section, and the Highest Flow Instability Indexes section.** Parser-side change with no impact on existing consumers (additive return values). Unblocks T-A5, T-A6, F-A14, F-C9. Effort: ~1–2 hours; medium risk because section formats vary slightly across SWMM 5.1 vs 5.2 and the parser has CP-1252-vs-UTF-8 encoding fallback to preserve.
- **Add `Total Elapsed Time` to the `.rpt` parser return.** Trivially extractable (single regex), enables T-C3 and gives SWMM-only sensitivity sweeps a runtime axis without depending on the toolkit's performance zarr.
- **Consider authoring a project-scope stipulation "SWMM continuity-error band annotations follow Vol II §3.4"** if/when the user commits to F-A7a/F-A7b. Codifies green/yellow/red thresholds so renderers don't drift.
- **The `flow_balance_error_percent` per-node field is currently parsed but never surfaced in any toolkit zarr or report.** Either expose it in the per-scenario zarr (`SWMM_node_summary.flow_balance_error_percent`) or have F-A13/T-B2 parse the `.rpt` directly. Cleaner to expose in the zarr.
- **Cross-mode (CC) coupling-correctness check (proposed F-A16) is a higher-value smell-detection item than several seed `·` items.** The main agent should consider whether to elevate it to ★ after the catalog merge.
- **F-A12 (top-K node depth time series) is currently `·` in the seed; specialist recommends ★.** If the user agrees, the renderer should also annotate node rim elevation as a horizontal line so HGL-above-rim is visible at a glance.
- **The toolkit's `sims/{event_id}/` layout uses a stable event-slug.** Worth considering whether the report's per-event tabular labels should use the slug (human-readable, e.g., `year.9_event_type.compound_event_id.1`) rather than `event_iloc=0..N`. The slug carries semantic meaning a reviewer can actually parse.
- **Substrate gap (web-cache/pdf-extract)**: SWMM Applications Manual (EPA/600/R-09/077) — carries example reports and figure conventions; not currently in substrate.
- **Substrate gap (web-cache/pdf-extract)**: a curated SWMM-visualization-conventions reference (color thresholds, acceptance bands, figure-type conventions for stormwater-engineering reports). Candidate sources: WaPug "Code of Practice for the Hydraulic Modelling of Sewers" (UK), EPA SWMM Applications Manual, WEF "Manual of Practice No. 28: Existing Sewer Evaluation and Rehabilitation."
- **Substrate gap (knowledge-file)**: SWMM `.rpt` text-format authoritative spec — every section header and field name normalized across versions. Synthesizable from User's Manual Appendix E + Stormwater-Management-Model `report.c` + `error.txt`. Reduces implementation risk on the proposed parser-extension items (T-A5, T-A6, F-A14).

### staleness checker (surfaced 2026-04-30)

- **`scripts/validate/check_substrate_staleness.py` should accept a project-pin reference** (e.g., read `TRITONSWMM_branch_key` from a project's `system_config.yaml`) and check whether the substrate's `ref` matches THAT pin rather than upstream HEAD. Today the script only knows about upstream HEAD, which produces false-positive HARD_STALE alarms for any project that deliberately pins to an older ref. Encountered in the visualization-and-reporting session 2026-04-30 — wasted effort on a "substrate refresh" gate that was actually a no-op. Concrete proposal: add `--against-pin <ref>` CLI flag (or `--from-system-config <path>` that auto-extracts the pin), default behavior unchanged for backward compatibility, but document the new flag in the staleness-gate protocol so callers (especially `/invoke-specialist` Step 5's substrate-staleness gate) can use it for project-pinned tools.

### triton-swmm-toolkit-specialist (plan-purpose, 2026-04-30)

- **Collapse `sensitivity_benchmarking.py` v1 + v2 to a single module** — at v2 plan completion, the active-Phase-6 module and v2-Phase-7's `sensitivity_benchmarking_v2.py` both exist (per the no-deletions rule). A future cleanup plan should collapse them once v2 is fully validated.
- **Promote "Figure format by artifact type" assumption to a stipulation** — the active plan's assumptions section captures the raster-PNG/vector-SVG rule informally; v2 should formalize as `figure format by artifact type.md` stipulation.
- **Automate the cell-wise-summary CF metadata refresh check** — the v2-Phase-0 DDM marks the CF-conventions stipulation as `[REFRESH]` candidate. A linter asserting every consolidated DataTree variable has CF metadata would catch future drift.
- **Per-renderer `params.report_cfg_hash` for narrow re-render** — the active plan defers this to v1.5; v2 plan does not depend on it but would benefit from narrower re-render triggers as the catalog grows. Promote to v1.5 follow-up.
- **Sensitivity-mode forward-compatibility test** — the v2 plan's sensitivity-mode scaffolding only implements `benchmarking_study`; a smoke test asserting the conditional dispatch correctly skips C.2 when `mode != "benchmarking_study"` would catch future-mode regressions.
- **Performance-zarr documentation in architecture doc** — v2-Phase-0 adds a per-rank sibling zarr; the architecture doc data-schema section should grow a per-zarr table documenting every variable's purpose, dtype, dims, and coordinate semantics.
- **Idea-file generator script** — R-V2-12 requires writing 9 idea files via `/capture-idea`. A wrapper that takes the catalog's "Tier-2 deferred" rows and emits draft idea files in batch would reduce the v2-completion overhead.
- **Cross-mode (CC) detection logic centralization** — Phase 5 extends `_get_enabled_model_types()` to emit `CC` when ≥2 model types active; the same logic applies to Snakefile rule generation, HTML wrapper conditionals, and renderer dispatch. A single source-of-truth helper avoids drift.

### plan-review specialists (swmm + gis + xarray, 2026-05-01)

**From swmm-specialist plan-review:**

- **F-FU-1 (`swmm_api` toolkit-wide adoption)** — even with v2's F-OH-1 partial overhaul (only the new diagnostic accessors route through `swmm_api`), the toolkit retains a hand-rolled `parse_rpt_single_pass` for the legacy node/link/system parsing path. Long-term, a unified `swmm_api.SwmmReport`-based parser would let the toolkit drop ~600 lines of regex-based RPT parsing and gain swmm_api's 11-WARNING taxonomy, `analyse_duration`, and version-tracking. Broader than v2's stated objective; capture for a future refactor plan.
- **F-FU-2 (Toolkit-level `INERTIAL_DAMPING` / `COURANT_FACTOR` policy across SWMM versions)** — SWMM 5.2.3→5.2.4 changed `InertDamping` default (SOME→PARTIAL) and `CourantFactor` default (0→0.75). The toolkit's defaults are already aligned with 5.2.4 per CLAUDE.md Gotchas, but no test asserts the pinning. A toolkit-wide policy + a `tests/test_swmm_options_pin.py` regression would catch silent default drift on future SWMM upgrades. Orthogonal to v2's reporting objective.

**From gis-specialist plan-review:**

- **Flag 6 — `geojson preferred spatial format` stipulation promotion** — the active master plan's Assumptions section captures the "GeoJSON-preferred + uniform-CRS-required" rule informally; promote it to a durable stipulation file at `library/docs/stipulations/TRITON-SWMM_toolkit/geojson preferred spatial format.md` post-active-closeout (active plan must reach `plan_status: completed` first, since the stipulation references a rule the active plan owns). Specialist's promotion deferred to v2 plan completion.
- **Flag 7 — `_swmm_gis_layers.py` SWMM-node export reuse for Fig-A16** — Fig-A16's GIS contract (per Spec 1) builds the SWMM-flooded-node GeoDataFrame from `.inp` `[COORDINATES]` re-parsed at render time. The active plan's `_swmm_gis_layers.py` already exports SWMM nodes as a GeoJSON sidecar at scenario-prep time. A future refactor could route Fig-A16's node source to the GeoJSON sidecar (single source of truth, persists provenance) rather than re-parsing `.inp`. Orthogonal to v2's stated objective.

**From xarray-specialist plan-review:**

- **Flag 8 — Performance-zarr documentation update post-D-V2-C resolution** — the D-V2-C dual-shape schema (per-rank + aggregated arrays in `performance_summary.zarr`) deserves a per-zarr documentation table in the toolkit architecture doc — a single place that lists every variable's purpose, dtype, dims, and coordinate semantics. Catches future drift when the schema grows. Architecture-doc update; orthogonal to v2's renderer-authoring objective.


