Per-experiment disk utilization across all combined experiments, one column per model (TRITON-SWMM / TRITON). Each row is a storage scope; cells show bytes on disk, with per-model totals in the footer. Derived deterministically from each child bundle's ``_status/_du.json``.
