Per-experiment validation-check roll-up across all combined experiments. Most cells are one experiment's pass/fail for one check; hover a FAIL for its summary. Rows marked *(derived across experiments)* are computed at combine time from the whole combined set rather than reported by any single experiment. See the experiment's own "Errors and Warnings" section for full detail.
