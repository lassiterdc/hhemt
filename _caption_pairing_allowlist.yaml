# An entry exempts ONE (renderer, caption) PAIR from the caption-pairing
# requirement, for the EXACT renderer content recorded in renderer_sha256.
#
# Keyed by the PAIR, not by the renderer: a module-keyed exemption would let one
# justification cover every caption that module owns -- six, for
# eda_compute_sensitivity -- which is a blanket rather than an exemption. One
# justification covers one re-read. The verbosity is deliberate: it makes "one
# sentence covers six documents" unwriteable.
#
# The digest makes the exemption EXPIRE. Change the renderer again and it stops
# matching, so the guard fires and someone re-reads the caption. Compare
# _layout_relevant_files.yaml's non_breaking_allowlist, whose scenario.py entry
# has already accreted "Two collapsed non-breaking changes" because a path-keyed
# exemption never expires -- the permanent-hole outcome, already realised in the
# sibling machinery this guard is modelled on.
unpaired_allowlist:
  - renderer: sensitivity_benchmarking
    caption: src/hhemt/report_templates/captions/sensitivity_benchmarking.rst
    renderer_sha256: 5e58a7d124e0907803e36994e67a823608a5974e04494f2dd6029a58f56db71a
    justification: >-
      Iteration-8 caption-geometry group (D1.2 width declaration, D1.3
      content_width_px adoption, R5.6 literal-subtraction retirement), plus a
      whole-file ruff-format pass. Caption re-read: it describes the dependent
      variable, aggregation mode, SWMM-only routing and the wallclock-reduction
      stipulation, and makes no structural claim about panels, columns or
      caption geometry, so none of those changes falsifies it. Looked; no
      change needed.
      RE-RECORDED for the sa_id identity-column repair: the renderer changes are
      the .agg revert to _MEMBER_KEY, a _require_member_key boundary guard and
      its three call sites, the first-party import block moved to the canonical
      top position (four E402), four B007 loop-variable renames, and an
      in-figure annotation repair. Caption RE-READ in full: it describes the
      dependent variable, aggregation mode, SWMM-only routing, the
      wallclock-reduction stipulation and the per-report axis auto-scaling. None
      of those is touched -- the identity column carries member provenance for
      hover and per-point recovery, not the plotted quantity, and no panel,
      column or axis semantics moved. The one caption-shaped defect found while
      re-reading was NOT in this .rst but in the renderer's own in-figure
      _bench_caption, which told the reader the hybrid digit was the MPI rank
      count while both builders emit n_omp_threads; that string is FIXED in this
      same change rather than allowlisted. Looked; no change needed to this
      file.
  - renderer: cross_experiment_intercomparison_maps
    caption: src/hhemt/report_templates/captions/cross_experiment_intercomparison_maps.rst
    renderer_sha256: 882640f7f4e440bddad564484d61b4dbe149cd60e25a83e564105459fddb2fc1
    justification: >-
      Iteration-8 annotation-carrier group (D3 annotation split and the
      title-collision fix), plus a whole-file ruff-format pass. Caption re-read:
      it describes diff-map semantics (resume minus clean over byte-identity
      classes), the RdBu direction convention, and the variable-dt SWMM
      final-period truncation. All three are data claims; the change moved
      annotation anchoring only. Looked; no change needed.
      RE-RECORDED for the config-labels leaf extraction: the renderer's only
      change is an import re-point of _derive_config_label from
      hhemt.eda._config_diff to the new hhemt.config_labels leaf. The BOUND
      NAME, both call sites and the function body are unchanged, so no panel,
      table, colour scale or caveat this caption describes can move. Caption
      re-read in full; no change needed. NOTE: the
      cross-experiment RESTRUCTURE is still open on a user question, so this
      exemption expires with the next renderer change by construction.
  - renderer: errors_and_warnings
    caption: src/hhemt/report_templates/captions/errors_and_warnings.rst
    renderer_sha256: 1632cee63a3bce0f8406c1b97abf207ca642694b375c888e64c59196b9ad5491
    justification: >-
      docs-and-experiment-system merge. Caption re-read against a MEASURED diff
      rather than a read-through. The merge-authored delta to this renderer --
      taken from the index and whitespace-normalised -- is FOUR lines, all of
      them the sub-analysis -> member vocabulary rename: two _CHECK_VOCABULARY
      description strings, and a local variable renamed from sa_id to member_id.
      The section-label set was extracted from HEAD, MERGE_HEAD and the merged
      index and is BYTE-IDENTICAL across all three, so the caption's claim of
      four sections (system-level, aggregate per-scenario, granular
      per-scenario, resource-utilization) is exactly as true after the merge as
      before it. No column moved, no section was added or removed, no unit
      changed. Separately verified that member_id = d.get("sa_id", "") is a
      cosmetic half-rename and not a defect: analysis_validation.py emits the
      "sa_id" detail key 8 times at HEAD, 8 at MERGE_HEAD and 8 in the merged
      index, so the producer key never moved. Looked; no change needed.
      Digest recorded AFTER the whole-file ruff-format pass that rides this same
      commit -- recording it before would have gone stale on arrival and the
      guard would have fired again with a stale-exemption message.
  - renderer: cross_experiment_disk_utilization
    caption: src/hhemt/report_templates/captions/cross_experiment_disk_utilization.rst
    renderer_sha256: 6f92bab4587a3a7f54a5701df7a7f7961eeaaa45648ffd0a0e3389ea5a35c140
    justification: >-
      docs-and-experiment-system merge. FORMATTING ONLY -- discharged by measurement,
      not by judgement. This renderer's abstract syntax tree, parsed from the working
      tree, is IDENTICAL to its AST at MERGE_HEAD (and differs from HEAD, because the
      merge took the other branch's version of this file wholesale). The only textual
      difference from MERGE_HEAD is the whole-tree ruff-format pass riding this same
      commit. A formatting change cannot alter a column, a unit, a section, or any
      other claim a caption can make, so this caption cannot have gone stale and a
      re-read has no possible finding. The renderer's semantics were reviewed on the
      branch that authored them. Reproduce with:
      python -c "import ast,subprocess;
      f='src/hhemt/report_renderers/cross_experiment_disk_utilization.py';
      g=lambda r: ast.dump(ast.parse(subprocess.run(['git','show',r+':'+f],capture_output=True,text=True).stdout));
      print(ast.dump(ast.parse(open(f).read()))==g('MERGE_HEAD'))"
      Digest recorded AFTER the reformat, so the exemption expires on the next real
      change to this file rather than on arrival.
  - renderer: metadata
    caption: src/hhemt/report_templates/captions/metadata.rst
    renderer_sha256: 5fccf9b946a2d380cc3feb1ab0f9f9149cba0636f42974ce3aa88b40d7841041
    justification: >-
      docs-and-experiment-system merge. FORMATTING ONLY -- discharged by measurement,
      not by judgement. This renderer's abstract syntax tree, parsed from the working
      tree, is IDENTICAL to its AST at MERGE_HEAD (and differs from HEAD, because the
      merge took the other branch's version of this file wholesale). The only textual
      difference from MERGE_HEAD is the whole-tree ruff-format pass riding this same
      commit. A formatting change cannot alter a column, a unit, a section, or any
      other claim a caption can make, so this caption cannot have gone stale and a
      re-read has no possible finding. The renderer's semantics were reviewed on the
      branch that authored them. Reproduce with:
      python -c "import ast,subprocess;
      f='src/hhemt/report_renderers/metadata.py';
      g=lambda r: ast.dump(ast.parse(subprocess.run(['git','show',r+':'+f],capture_output=True,text=True).stdout));
      print(ast.dump(ast.parse(open(f).read()))==g('MERGE_HEAD'))"
      Digest recorded AFTER the reformat, so the exemption expires on the next real
      change to this file rather than on arrival.
  - renderer: per_analysis_summary
    caption: src/hhemt/report_templates/captions/per_analysis_summary_table.rst
    renderer_sha256: 5101456a3a8a06645385ef08bb0756c2f25677e5f275173b0862d9ed3dd505d8
    justification: >-
      docs-and-experiment-system merge. FORMATTING ONLY -- discharged by measurement,
      not by judgement. This renderer's abstract syntax tree, parsed from the working
      tree, is IDENTICAL to its AST at MERGE_HEAD (and differs from HEAD, because the
      merge took the other branch's version of this file wholesale). The only textual
      difference from MERGE_HEAD is the whole-tree ruff-format pass riding this same
      commit. A formatting change cannot alter a column, a unit, a section, or any
      other claim a caption can make, so this caption cannot have gone stale and a
      re-read has no possible finding. The renderer's semantics were reviewed on the
      branch that authored them. Reproduce with:
      python -c "import ast,subprocess;
      f='src/hhemt/report_renderers/per_analysis_summary.py';
      g=lambda r: ast.dump(ast.parse(subprocess.run(['git','show',r+':'+f],capture_output=True,text=True).stdout));
      print(ast.dump(ast.parse(open(f).read()))==g('MERGE_HEAD'))"
      Digest recorded AFTER the reformat, so the exemption expires on the next real
      change to this file rather than on arrival.
  - renderer: scenario_status_appendix
    caption: src/hhemt/report_templates/captions/scenario_status_appendix.rst
    renderer_sha256: 3d7c2682cfbc438accfcf1d48f6b5507d62134be3d57788c4627fdee86b7e54e
    justification: >-
      docs-and-experiment-system merge. FORMATTING ONLY -- discharged by measurement,
      not by judgement. This renderer's abstract syntax tree, parsed from the working
      tree, is IDENTICAL to its AST at MERGE_HEAD (and differs from HEAD, because the
      merge took the other branch's version of this file wholesale). The only textual
      difference from MERGE_HEAD is the whole-tree ruff-format pass riding this same
      commit. A formatting change cannot alter a column, a unit, a section, or any
      other claim a caption can make, so this caption cannot have gone stale and a
      re-read has no possible finding. The renderer's semantics were reviewed on the
      branch that authored them. Reproduce with:
      python -c "import ast,subprocess;
      f='src/hhemt/report_renderers/scenario_status_appendix.py';
      g=lambda r: ast.dump(ast.parse(subprocess.run(['git','show',r+':'+f],capture_output=True,text=True).stdout));
      print(ast.dump(ast.parse(open(f).read()))==g('MERGE_HEAD'))"
      Digest recorded AFTER the reformat, so the exemption expires on the next real
      change to this file rather than on arrival.
  - renderer: system_overview
    caption: src/hhemt/report_templates/captions/system_map.rst
    renderer_sha256: 43e350a7f310d742fcdadcb635cedbd11bf5a93be0c87208fd048c103aac974f
    justification: >-
      docs-and-experiment-system merge. FORMATTING ONLY -- discharged by measurement,
      not by judgement. This renderer's abstract syntax tree, parsed from the working
      tree, is IDENTICAL to its AST at MERGE_HEAD (and differs from HEAD, because the
      merge took the other branch's version of this file wholesale). The only textual
      difference from MERGE_HEAD is the whole-tree ruff-format pass riding this same
      commit. A formatting change cannot alter a column, a unit, a section, or any
      other claim a caption can make, so this caption cannot have gone stale and a
      re-read has no possible finding. The renderer's semantics were reviewed on the
      branch that authored them. Reproduce with:
      python -c "import ast,subprocess;
      f='src/hhemt/report_renderers/system_overview.py';
      g=lambda r: ast.dump(ast.parse(subprocess.run(['git','show',r+':'+f],capture_output=True,text=True).stdout));
      print(ast.dump(ast.parse(open(f).read()))==g('MERGE_HEAD'))"
      Digest recorded AFTER the reformat, so the exemption expires on the next real
      change to this file rather than on arrival.
  - renderer: cross_experiment_compatibility
    caption: src/hhemt/report_templates/captions/cross_experiment_compatibility.rst
    renderer_sha256: d8ca3825cbd41486d54601632bdcd6315319c86c7a84467cafeed804ce06ba5b
    justification: >-
      docs-and-experiment-system <- sidequest-clearing-08-30 merge. FORMATTING ONLY --
      discharged by measurement, not by judgement, and following the AST-equivalence
      pattern the sibling entries in this file established. This renderer's abstract
      syntax tree, parsed from the working tree, is IDENTICAL to its AST at BOTH
      MERGE_HEAD and HEAD -- a stronger result than the sibling entries, which differ
      from HEAD because the merge took the other branch's file wholesale. Here the two
      branches' versions are semantically the same file and differ only in the pinned
      ruff-format v0.6.0 reflow (conditional expressions parenthesised, a string
      concatenation and a set literal re-wrapped). A formatting change cannot alter a
      column, a unit, a section, or any other claim a caption can make, so this caption
      cannot have gone stale and a re-read has no possible finding. Reproduce with:
      python -c "import ast,subprocess;
      f='src/hhemt/report_renderers/cross_experiment_compatibility.py';
      g=lambda r: ast.dump(ast.parse(subprocess.run(['git','show',r+':'+f],capture_output=True,text=True).stdout));
      print(ast.dump(ast.parse(open(f).read()))==g('MERGE_HEAD'))"
      Digest recorded AFTER the reformat, so the exemption expires on the next real
      change to this file rather than on arrival. This renderer was the one the
      authoring branch's own sweep missed; its siblings are allowlisted above.
  - renderer: cross_experiment_intercomparison
    caption: src/hhemt/report_templates/captions/cross_experiment_intercomparison.rst
    renderer_sha256: 801a266b2893eda088131064678016e7b0837823448d2462203cc61f5dde5b49
    justification: >-
      Tree-wide ruff pass over the merged file set. IMPORT-ONLY -- discharged by
      measurement, not by judgement. The sibling entries in this file prove
      formatting-only changes via whole-AST equivalence; that instrument cannot be
      used here, because REMOVING an import necessarily changes the AST. The
      correct proof is two-part and both parts were run. (1) The only imported name
      that changed is `_html` (F401, `import html as _html`), REMOVED, and it has
      ZERO references in the module: an ast.walk finds 0 Name nodes and 0 Attribute
      value-nodes bearing that identifier. Note that a plain `grep -c _html`
      returns 7 and is the WRONG instrument -- every hit is a substring of a
      different identifier (`build_html_document`, `_build_intercomparison_html`,
      `_plain_html`, `body_heading_html`), none of them the bare alias. (2) With
      imports stripped from every body list, the module's AST is IDENTICAL to its
      AST at HEAD, so no rendering statement moved. A caption cannot be falsified
      by the removal of a name nothing reads. Digest recorded AFTER the
      ruff-format pass riding this same commit, so the exemption expires on the
      next real change to this renderer rather than on arrival.

  - renderer: per_sim_peak_flood_depth
    caption: src/hhemt/report_templates/captions/per_sim_peak_flood_depth.rst
    renderer_sha256: 5f2f050945fddd676c792d3bc3ba6153c0bd677ae66b01cf8caafdf96496c181
    justification: >-
      Tree-wide ruff pass over the merged file set. IMPORT-ONLY -- discharged by
      measurement, not by judgement. The change is an I001 REORDER of one
      function-local import (`report_renderers._model_arms.resolve_arm_group`)
      within its own block, so whole-AST equivalence cannot hold (node order moved)
      even though nothing semantic did. Two measurements discharge it. (1) The SET
      of imported names is identical before and after -- zero added, zero removed
      -- so no name this module binds has changed. (2) With imports stripped from
      every body list, the module's AST is IDENTICAL to its AST at HEAD, so no
      panel, colour scale, tick derivation or emitted string moved. The caption
      describes the three-panel depth / WSE / event-hydrology figure and its data
      sources; none of that is reachable by reordering an import. Digest recorded
      AFTER the ruff-format pass riding this same commit, so the exemption expires
      on the next real change to this renderer rather than on arrival.
