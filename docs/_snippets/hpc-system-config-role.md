Cluster-bound work needs a third config: the **HPC-system config**, which describes
the machine rather than the study. It carries the partitions and their caps, the
account to charge, and the container settings if you use one. Local runs need only
the system and analysis configs.
