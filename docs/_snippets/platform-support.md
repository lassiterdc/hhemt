**Simulations run on Linux only.** The coupled solver is built and executed through a
Linux toolchain, so a simulation runs natively on Linux or under WSL on Windows. On
macOS, and on Windows outside WSL, you can install the package and work with results
that were produced elsewhere, but you cannot run a simulation.
