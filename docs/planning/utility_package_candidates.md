# Utility Package Candidates

Functions and classes in this codebase with no domain-specific logic that are candidates for extraction into a future shared pip-installable utility package. When porting a function to ss-fha or another project, record it here so the two implementations can be consolidated later.

## Format

| Function/Class | Module | ss-fha Analog | Notes |
|---|---|---|---|
| _(add entries as discovered)_ | | | |
